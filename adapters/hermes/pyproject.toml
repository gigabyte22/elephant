[project]
name = "hermes-memory-elephant-dev"
version = "0.1.0"
description = "Dev harness for the elephant hermes-agent memory provider (the plugin itself is stdlib-only)"
requires-python = ">=3.10"

[dependency-groups]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
